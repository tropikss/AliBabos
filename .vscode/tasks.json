{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Tracker",
            "type": "shell",
            "command": "python ${workspaceFolder}/tracker.py start \"Dev ${workspaceFolder}\"",
            "runOptions": { "runOn": "folderOpen" }
        }
    ]
}